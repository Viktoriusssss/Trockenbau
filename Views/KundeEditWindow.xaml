<Window x:Class="ModernWPFApp.Views.KundeEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{Binding WindowTitle}"
                 Height="700"
         Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,20" Padding="16">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AccountEdit" 
                                       Width="24" 
                                       Height="24" 
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="{Binding WindowTitle}" 
                          FontSize="18" 
                          FontWeight="Medium" 
                          VerticalAlignment="Center"
                          Margin="8,0,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                                 <!-- Firmenname -->
                 <TextBox materialDesign:HintAssist.Hint="Firmenname * (erforderlich)"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"
                         Text="{Binding Kunde.FirmenName, UpdateSourceTrigger=PropertyChanged}"/>

                                 <!-- Ansprechpartner -->
                 <TextBox materialDesign:HintAssist.Hint="Ansprechpartner * (erforderlich)"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"
                         Text="{Binding Kunde.Ansprechpartner, UpdateSourceTrigger=PropertyChanged}"/>

                                 <!-- Straße -->
                 <TextBox materialDesign:HintAssist.Hint="Straße * (erforderlich)"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"
                         Text="{Binding Kunde.Strasse, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- PLZ and Ort in one row -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                                         <TextBox Grid.Column="0"
                             materialDesign:HintAssist.Hint="PLZ * (erforderlich)"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Text="{Binding Kunde.PLZ, UpdateSourceTrigger=PropertyChanged}"/>

                                         <TextBox Grid.Column="2"
                             materialDesign:HintAssist.Hint="Ort * (erforderlich)"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Text="{Binding Kunde.Ort, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                                 <!-- Telefon -->
                 <TextBox materialDesign:HintAssist.Hint="Telefon * (erforderlich)"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"
                         Text="{Binding Kunde.Telefon, UpdateSourceTrigger=PropertyChanged}"/>

                                 <!-- Email -->
                 <TextBox materialDesign:HintAssist.Hint="Email * (erforderlich)"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"
                         Text="{Binding Kunde.Email, UpdateSourceTrigger=PropertyChanged}"/>

                

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}"
                          Foreground="Red"
                          TextWrapping="Wrap"
                          Margin="0,0,0,16"
                          Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <materialDesign:Card Grid.Row="2" Margin="0,20,0,0" Padding="16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="ABBRECHEN"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="0,0,8,0"
                        Command="{Binding CancelCommand}"/>

                                 <Button Content="SPEICHERN"
                         Style="{StaticResource MaterialDesignRaisedButton}"
                         Command="{Binding SaveCommand}"
                         IsEnabled="{Binding IsValid}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
