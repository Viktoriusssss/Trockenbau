<UserControl x:Class="ModernWPFApp.Views.AufmassPositionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:ModernWPFApp.Converters">

    <UserControl.Resources>
        <converters:UnitConverter x:Key="UnitConverter"/>
        <converters:EuroConverter x:Key="EuroConverter"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header and Actions -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,16" Padding="16">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FormatListNumbered" Width="24" Height="24" 
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBlock Text="Aufmaß Positionen" FontSize="20" FontWeight="Medium" 
                              VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" 
                           Orientation="Horizontal" 
                           HorizontalAlignment="Right">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Margin="8,0,0,0"
                            ToolTip="Neue Position hinzufügen"
                            Command="{Binding AddPositionCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="16" Height="16"/>
                            <TextBlock Text="Neu" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>

        <!-- Data Grid -->
        <materialDesign:Card Grid.Row="1" 
                            materialDesign:ShadowAssist.ShadowDepth="Depth2">
            <DataGrid x:Name="PositionDataGrid"
                     materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                     materialDesign:DataGridAssist.ColumnHeaderPadding="8"
                     AutoGenerateColumns="False"
                     CanUserAddRows="False"
                     CanUserResizeColumns="False"
                     CanUserReorderColumns="False"
                     HeadersVisibility="All"
                     SelectionMode="Single"
                     SelectionUnit="FullRow"
                     IsReadOnly="True"
                     ItemsSource="{Binding Positionen}"
                     SelectedItem="{Binding SelectedPosition, UpdateSourceTrigger=PropertyChanged}"
                     GridLinesVisibility="None">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Height" Value="50"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                                <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth1"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryHueLightBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth2"/>
                                <Setter Property="FontWeight" Value="Medium"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="IsTabStop" Value="False"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                <Setter Property="Background" Value="Transparent"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Padding" Value="12,12"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Height" Value="45"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Pos." 
                                       Binding="{Binding Position}" 
                                       Width="80"/>
                    <DataGridTextColumn Header="Beschreibung" 
                                       Binding="{Binding Beschreibung}" 
                                       Width="*"/>
                    <DataGridTextColumn Header="Länge" 
                                       Binding="{Binding Länge, Converter={StaticResource UnitConverter}}" 
                                       Width="100"/>
                    <DataGridTextColumn Header="Breite" 
                                       Binding="{Binding Breite, Converter={StaticResource UnitConverter}}" 
                                       Width="100"/>
                    <DataGridTextColumn Header="Höhe" 
                                       Binding="{Binding Höhe, Converter={StaticResource UnitConverter}}" 
                                       Width="100"/>
                    <DataGridTextColumn Header="Std" 
                                       Binding="{Binding Std, Converter={StaticResource UnitConverter}}" 
                                       Width="80"/>
                    <DataGridTextColumn Header="Stück" 
                                       Binding="{Binding Stück, Converter={StaticResource UnitConverter}}" 
                                       Width="80"/>
                    <DataGridTextColumn Header="Lfm" 
                                       Binding="{Binding Lfm, Converter={StaticResource UnitConverter}}" 
                                       Width="80"/>
                    <DataGridTextColumn Header="Gesamt Menge" 
                                       Binding="{Binding GesamtMengeDisplay}" 
                                       Width="150"/>
                    <DataGridTextColumn Header="EP" 
                                       Binding="{Binding Einzelpreis, Converter={StaticResource EuroConverter}}" 
                                       Width="100"/>
                    <DataGridTextColumn Header="Gesamt" 
                                       Binding="{Binding Gesamt, Converter={StaticResource EuroConverter}}" 
                                       Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>


